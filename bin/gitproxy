#!/bin/sh

host=127.0.0.1
port=7070

# socat
# exec socat STDIO PROXY:$host:$1:$2,proxyport=8118

# corkscrew
# exec corkscrew $host $port $*

# nc
nc -x$host:$port -X5 $*

# connnect
# connect -S $host:$port "$@"
