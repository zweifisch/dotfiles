#!/bin/sh


_proxy=localhost

# socat
# _proxyport=8118
# exec socat STDIO PROXY:$_proxy:$1:$2,proxyport=$_proxyport

# corkscrew
_proxyport=7070
# exec corkscrew $_proxy $_proxyport $*

# nc
# nc -x$_proxy:$_proxyport -X5 $*

# connnect
connect -S 127.0.0.1:7070 "$@"
